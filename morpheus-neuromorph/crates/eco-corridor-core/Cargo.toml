[package]
name = "eco-corridor-core"
version = "0.1.0"
edition = "2021"
description = "Indigenous Eco-Corridor Map core types for SNC-aligned, non-actuating governance."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
